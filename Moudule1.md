學習筆記- AI agent 實戰-Dify × LowCode × RAG
TOEIC英文單字學習APP

前言
	由於正在準備TOEIC，突發奇想可以製作一個TOEIC英文單字學習APP，提昇學習效果，經過資料搜尋，發現有很多AI工具可以使用，以LowCode，甚至NoCode即可快速實現APP 的POC到之後的實際部署。以下是AI搭配+Dify實現的過程記錄。

儘管已有很多的Dify書籍或課程，我決定用Gemini + Dify來實現，過程中會有版本或不清楚的地方，只要用Gemni大部份都可以修正，但是有些部分仍需要對Python程式有基本了解，才有辦法處理。可以這麼說，愈複雜的規格，即使搭配AI或Dify這種平台，仍然需要撰寫部份程式。
在平台發佈的程式，單字主題 -> 開始對話-> 打 go , 按  如對話太多或想全部重來，可以選右上角…的地方然後重置對話 
https://udify.app/chat/WEmkzfODxUpICPcu
       Step 1 : 給Gemini 規格書Prompt
上傳一份大約15類型100個單字的多益高頻單字表 , 用DIFY實作英文單字學習 APP, 規格如下:每次由某一類型選三個單字, 生成單字解析、K發音、KK 音標、詞性、中文翻譯、實用例句， 接下來考這三個單字的意思, 記錄學習成果, 若正確,顯示答案正確的圖像或訊息,之後則不再出現為考題, 若錯誤, 顯示答案錯誤的圖像或訊息，下次可以再出現在考題中





準備工作：將 100 個單字上傳至 Dify 知識庫 (RAG 知識庫)
首先，請將上一輪為您整理好的 100 個單字儲存為 toeic_vocab.csv（編碼請務必選擇 UTF-8 格式），然後在 Dify 後台點選 「知識庫 (Knowledge)」 -> 「建立知識庫」 -> 「上傳 CSV 檔案」。
Dify 工作流架構設計 (建議使用 Chatflow 或 Workflow)
要在 Dify 中做到「記住學習成果、排除已懂單字」，最有效率的做法是利用 Dify 內建的 「對話變數 (Conversation Variables)」功能。
請在 Dify 內建立一個名為 mastered_words 的對話變數（型態為 String 字串或 Array 陣列，預設值為空），用來存放使用者已經答對的單字清單。








流程節點規劃：
📌 節點一：開始與主題選擇 (Start Node)
輸入變數：建立一個下拉選單 {{selected_category}}，讓使用者選擇 15 個主題之一（例如：商業會議、人力資源等）。
📌 節點二：隨機抽題與解析 (LLM Node - 老師解說)
核心 Prompt 設定：
Markdown
# Role: 多益金牌講師
# Task: 請從知識庫中，針對【{{selected_category}}】主題，隨機挑選 3 個「不在」已掌握清單【{{mastered_words}}】中的單字。

# Output Format:
請嚴格依繁體中文格式輸出這 3 個單字的解析：
### 本次核心單字學習
1. **[單字]**
   - **KK 音標**：[音標]
   - **詞性**：[詞性]
   - **中文翻譯**：[中文翻譯]
   - **實用例句**：[英文例句] (繁體中文翻譯)
2. ... (以此類推)

---
### 即時多益小測驗
請依序回答剛才這三個單字的意思：
1. 「[中文翻譯1]」的英文單字拼寫是？
2. 「[中文翻譯2]」的英文單字拼寫是？
3. 「[中文翻譯3]」的英文單字拼寫是？









📌 節點三：使用者輸入答案 (Answer Node)
等待使用者回覆 3 個單字的拼寫。
📌 節點四：答案批改與條件判斷 (LLM + Conditional Node)
利用一個 LLM 節點比對正確答案與使用者的輸入（不區分大小寫）。
圖像與訊息顯示邏輯：
若答對：輸出 🟢【答案正確！】的成功訊息或綠色打勾標誌，並透過 Dify 的 「變數賦值 (Variable Assignation)」 節點，將該單字串接追加到 {{mastered_words}} 中。
若答錯：輸出 🔴【答案錯誤，再接再厲！】的錯誤訊息或紅色叉叉圖像，且不更新變數（使其下次仍有機會被抽中）。







進階規格優化建議 (前端視覺與圖像)
動態圖像顯示： 在 Dify 的 Markdown 輸出中，您可以直接使用標準的 Markdown 圖片語法或 Emoji 來提升視覺效果。例如：
答對時輸出：![Correct](https://img.icons8.com/color/96/checked--v1.png) \n ### 恭喜你，答對了！此單字已移出題庫。
答錯時輸出：![Wrong](https://img.icons8.com/color/96/cancel--v1.png) \n ### 太可惜了，拼寫有誤！此單字下次還會再考你喔。
單字發音（K發音）： Dify 內建有 TTS (Text-to-Speech) 語音播放功能。您可以在 LLM 節點或對話框設定中開啟語音功能，當 App 產生出單字解析時，使用者點選播放鍵即可直接聆聽標準的英文發音。
這套架構能完美嵌入 Dify 且不需要撰寫複雜的外部資料庫後端程式。如果您在佈署工作流的「變數賦值節點」遇到設定上的問題，隨時告訴我，我們可以針對該節點做更深入的細節拆解

由此整理 單字 : (註: copy & paste TOEIC 參考資料2)
 “本文特別整理出 15 大商業主題，精選多益高頻單字、多益常考單字與多益普及單字，並附上標準音標、例句及繁體中文翻譯，幫助您掌握最實用的多益英文單字。不論您是想突破 600 分，還是挑戰 900 分高分門檻，這份完整的多益單字大全都能為您提供清晰系統的學習架構。
目錄
I. 15個有用的多益單字主題
1. 商業會議（Meetings & Conferences）
2. 人力資源（Human Resources）
3. 行銷與廣告（Marketing & Advertising）
4. 合約與協議（Contracts & Negotiations）
5. 財務與會計（Finance & Accounting）
……
Step  2: 給Gemini  15 大商業主題 TOEIC 單字資料，在此因為實驗字數不多, 故直接copy & paste, 如果是幾千字，則應該寫一個Parser程式來處理

這是一份非常完整且結構清晰的多益（TOEIC）核心單字文本！為了讓您能夠直接匯入 Dify 的知識庫，並建立流暢的「英文單字學習 APP」工作流，我已經將您提供的文字去蕪存菁，整理成標準的結構化資料表，並為您規劃好 Dify 的系統 Prompt（提示詞）設定。
15大主題・100+ 多益高頻單字表 (CSV 格式)
請複製下方代碼框內的所有內容，另存為 toeic_vocab.csv 檔案（編碼請選 UTF-8），即可直接上傳至 Dify 的「知識庫」：
程式碼片段
單字,詞性,KK音標,中文翻譯,主題分類,實用例句
agenda,n.,/əˈdʒendə/,議程,商業會議,The meeting agenda includes budget review and project updates.
conference,n.,/ˈkɒnfərəns/,會議；研討會,商業會議,The annual sales conference will be held in Singapore next month.
attendee,n.,/əˈtendiː/,出席者,商業會議,All attendees must register before the conference be




進階規格優化建議  都要實現; 需要非常詳細的step by step 2026 最新版Dify 操作Step 3 : 規格優化，要求Step 1回應的進階規格都要實現

沒問題！我們直接從零開始，一步一步手把手帶你在 Dify 後台將這個單字學習 App 建立起來。即使你以前完全沒用過 Dify，只要照著下面的步驟點選，就能完整實現自動排除、發音與答題圖像提示的所有進階規格。
我們將整個架構分為 「準備單字庫」 與 「建立對話工作流」 兩大階段。
第一階段：準備單字庫（知識庫）
首先，我們要讓 AI 擁有那 100 個多益單字的資料。
登入 Dify 後台，點選上方主選單的 「知識庫 (Knowledge)」。
點選右上角的 「建立知識庫」。
選擇 「上傳文本檔案」，並將前面為你整理好、存成 UTF-8 編碼的 toeic_vocab.csv 檔案拖曳上傳進來，點選「下一步」。



分段設定（重要）：
在「常規」模式下，依照以下步驟進行設定：
分段識別符號：請在這裡輸入 \n（如果系統允許直接按 Enter 鍵換行，也可以直接按一下 Enter，目的是讓系統知道「遇到換行就切分」）。
分段最大長度：請設定大約 150 到 250。
原因：我們上傳的 CSV 檔中，每一行（一個單字＋音標＋例句）大約只有 50 到 100 個字。把最大長度設為 150～250，可以確保「每一個單字獨立成為一個完整區塊」，而不會跟前後文的單字黏在一起。
分段重疊長度：請設定為 0。
原因：因為多益單字表每行都是獨立的單字，不需要像讀小說或文章一樣讓前後段落重疊保留上下文。
Top K : 使用預設值 3
原因：問題與答案通常很簡短，最精準的答案通常就在前 3 名，調太高反而會混入不相關的問答。
點選 「儲存並處理」，等待系統解析完成。完成後，記下這個知識庫的名稱（例如：多益高頻單字庫）。
第二階段：建立 Dify Chatflow（對話工作流）
我們要使用 Dify 最強大的 Chatflow（對話工作流） 來控制複雜的答題與變數邏輯。
步驟 0：建立新專案
點選 Dify 上方主選單的 「工作室 (Studio)」。
點選 「建立空白應用」。
選擇 「Chatflow (對話工作流)」（這比單純的 Chat 更有彈性，能自由連線節點），為它取個名字（如：多益單字高效學習機），然後點選「建立」。









步驟 1：開始（Start 節點）- 建立保險箱與分類
這是整個工作流的火車頭，我們要在這裡為系統裝上眼睛和記憶保險箱。
外部輸入變數：
點擊添加變數，命名為 selected_category (String / 必填)。這是使用者在前端網頁上用來選擇多益主題（如：商業會議、交通運輸）的下拉選單。
在 「輸入字段」 區域，點選右邊的 「+」 或 「新增」 按鈕。
此時會跳出一個選擇欄位的選單，請選擇 「Select (下拉選項)」。
接著填入以下設定：
欄位名稱 (Key / 變數名稱)：輸入 selected_category
標籤名稱 (Label / 顯示名稱)：輸入 請選擇多益單字主題
必填 (Required)：請勾選（確保使用者一定要選主題才能開始）。
填寫下拉選單的選項 (Options)：
在下方的選項輸入框中，將 15 個主題依序貼進去（每行一個，直接按 Enter 換行）：


對話變數（後台保險箱）：
在下方對話變數區域，點擊新增一個變數，命名為 mastered_words，型態選擇 Array[String]，初始值保留空白 []。
（這個保險箱非常關鍵，用來死死記住使用者哪些單字已經學會、不需要重複再背了！）

步驟 2：條件分支（IF/ELSE 節點）- 自動鐵軌轉轍器
火車一出發，就會撞上這座閘門。它會根據使用者當下輸入的文字內容（sys.query），自動判斷該走「出題路線」還是「批改路線」。
IF 分支（出題軌道）：
點擊新增條件，左邊選擇 開始 / query ➔ 操作選擇 「包含」 ➔ 右邊輸入 go。
點擊右側的 + 新增並列條件，左邊選擇 開始 / query ➔ 操作選擇 「包含」 ➔ 右邊輸入 開始。
關鍵設定：將這兩個條件上方的邏輯關係，切換成 或 (OR)。
連線拉法：將 IF 出口拉出一條實線，連向 ➔ 知識檢索 節點。
ELSE 分支（批改軌道）：
不需要任何設定。當使用者不是輸入 go 而是輸入單字答案時，火車會被自動踢進這裡。
連線拉法：將 ELSE 出口拉出一條實線，連向 ➔ 2_答案批改與圖像 節點。

步驟 3：1_抽題解說老師（上半段出題 LLM）- 嚴格的考卷印刷機
火車走 IF 路線，經過 知識檢索 撈出單字 CSV 後，就會進入這個大腦節點。
上下文設定 (Context)：確保它正確綁定著左邊 知識檢索 的輸出。
系統提示詞 (System Prompt)：（直接複製以下完美引入變數與排除清單的版本）
Plaintext
# Role: 多益英文講師 

# Context:
- 使用者目前已經完全熟練的單字陣列為：{{#開始.mastered_words#}}
- 本次測驗的指定主題類別為：{{#開始.selected_category#}}

# Task:
1. 請仔細檢視目前使用者已經熟練的單字陣列：{{#開始.mastered_words#}}。
2. 請從「知識庫檢索結果」中，針對本次指定主題【{{#開始.selected_category#}}】，隨機抽出 3 個「絕對不在」已熟練陣列中的全新多益單字。
3. 如果 {{#開始.mastered_words#}} 是空的，代表是第一輪測驗，請直接從該主題隨機抽出 3 個單字。

# Constraints:
- 嚴格比對：只要單字出現在 {{#開始.mastered_words#}} 中（無視大小寫），本次出題就絕對、完全不能再選它！請換別的單字。

# Output Format (請嚴格遵循繁體中文格式) 
### 本次核心單字學習 

1. **[單字1]**
   - KK 音標：[音標]
   - 詞性：[詞性]
   - 中文翻譯：[中文翻譯]
   - 實用例句：[英文例句] (中文翻譯)

2. **[單字2]**
   - KK 音標：[音標]
   - 詞性：[詞性]
   - 中文翻譯：[中文翻譯]
   - 實用例句：[英文例句] (中文翻譯)

3. **[單字3]**
   - KK 音標：[音標]
   - 詞性：[詞性]
   - 中文翻譯：[中文翻譯]
   - 實用例句：[英文例句] (中文翻譯)








### 即時多益小測驗 
請在下方的輸入框中，依序回答這三個單字的英文拼寫（請直接回覆，並用半形逗號隔開，例如: word1, word2, word3）： 

1. 「[中文翻譯1]」的英文是？ 
2. 「[中文翻譯2]」的英文是？ 
3. 「[中文翻譯3]」的英文是？
(貼心提醒：在 Dify 裡，複製進去後如果括號沒有變成彩色泡泡標籤，記得手動按 { 把 mastered_words 和 selected_category 重新點選出來喔！)










步驟 4：語音處理流（過濾器 + TTS）- 純英文聲學加工廠
這條路線負責把老師的繁體中文 Markdown 題目，脫胎換骨變成純英文音訊。
步驟 4-1：語音過濾器（Code 節點）
在左側輸入變數增添一組：命名為 llm_text，點擊右側 [𝑥] 指定指向 ➔ 1_抽題解說老師 / text。
Python 程式碼：將右側代碼編輯器清空，換上這段能完美抓出粗體字單字的強健代碼：
Python
import re

def main(llm_text: str) -> dict:
    if not llm_text:
        return {"pure_english": "Welcome"}
    # 2026-05-31: 精準抓取加粗的英文單字本體，過濾掉所有中文與雜訊
    words = re.findall(r'\*\*\[?([a-zA-Z\s\-]+)\]?\*\*', llm_text)
    if not words:
        clean_text = re.sub(r'[\u4e00-\u9fa5]|[\d\-\|\#\:\*\?\.（）\(\)\,\，\s]', ' ', llm_text)
        pure_english = ", ".join([w.strip() for w in clean_text.split() if len(w.strip()) > 1])
    else:
        pure_english = ", ".join([w.strip() for w in words])
    if not pure_english.strip():
        pure_english = "Ready, Let's start."
    return {"pure_english": pure_english}
連線拉法：從 1_抽題解說老師 拉一條實線連到這個 語音過濾器。
步驟 4-2：TEXT TO SPEECH（語音節點）
展開面板，在 「文字內容 (Text)」 點擊 [𝑥]，指向 ➔ 語音過濾器 / pure_english。
模型選擇（極度重要）：點擊 Model 下拉選單，手動點選切換為 OpenAI 專用的語音模型 tts-1（千萬不要用預設的 gpt-4o-mini-tts，那會直接沒聲音）。
連線拉法：從 TEXT TO SPEECH 右側藍色輸出圓點拉出一條實線，直接向右長途跋涉，連到整個畫布最右側、獨立於分支外的「AI終端總管」左側輸入端。
步驟 5：2_答案批改與圖像（下半段批改流）- 帶歷史記憶的改卷官
當使用者輸入答案時，火車走 ELSE 路線會直接撞進這裡。
對話歷史 (Chat History)：在右側面板找到「對話歷史」功能，將它點擊開啟，並將最大輪數設定為 10。（如果沒開這個，老師就不記得前一輪出了什麼題目，會直接退化成瞎聊 AI 喔！）
系統提示詞 (System Prompt)：
Plaintext
# Role
你是一位嚴格且專業的新制多益（TOEIC）閱卷與批改老師。

# Task
請根據「對話歷史」中【1_抽題解說老師】剛剛出的那 3 個多益單字與中文翻譯，來批改學生當下提交的單字拼寫答案。

# Input
學生當下提交的答案：{{#sys.query#}}

# Rules
1. 嚴格比對學生輸入的拼寫（無視大小寫）是否與出題的 3 個單字完全一致。
2. 格式化輸出批改結果（必須明確指出哪一題對、哪一題錯，並給出正確答案）。
3. 你的輸出文本中，最後一行必須嚴格包含此標籤格式（以便後續 Python 記錄熟記單字）：
   PASSED_WORDS:[正確的單字1, 正確的單字2]
   (如果全部答錯，請寫 PASSED_WORDS:[])

# 結尾引導規範
批改完成後，請絕對不要說「隨時告訴我、有問題再問我」等客套對話。
請一律使用以下這段話作為你的回答結尾，引導學生進入下一輪：








恭喜完成這一輪挑戰！
準備好進入下一回測驗了嗎？請在下方輸入框直接打 **go**，老師就會立刻幫你抽下一組多益單字喔！
後續處理（數據寫入庫）：
連線結構：在 2_答案批改與圖像 的右邊串上一顆 程式碼執行 (Python) 節點（用來解析 PASSED_WORDS），接著再串上一顆 變數分配 (Variable Assigner) 節點。
變數分配面板點選動作：
目標變數：選擇對話變數中的 mastered_words（Array 型態）。
操作 (Operation)（徹底除錯關鍵 ）：請一定要點擊下拉選單，從「追加/擴展」切換成 「覆寫 (Overwrite)」（部分版本顯示為 「設定 (Set)」）。這能保證每次寫入的都是最新、最乾淨、去重後的單字庫。
值 (Value)：信心滿滿地按右側的 [𝑥]，在彈出選單點選 程式碼執行 ➔ 選擇它的輸出陣列（如 result_array 或 updated_list）。
連線拉法：從這個 變數分配 節點右側拉出一條實線，直接向右拉，連到最右側的「AI終端總管」左側輸入端。
步驟 6：AI終端總管（直接回覆節點）- 破除聲音禁令的公共終點站
這是本次架構最高明的一步！因為 Dify 的音訊流在 IF/ELSE 條件分支內部會被截斷，我們大膽地把原本分支內的所有直接回覆節點統統刪掉，在畫布最右側的公共區域，放上一個唯一的直接回覆積木。
節點更名：AI終端總管
連線外觀檢查：
它的左側此時應該只接到兩條實線：一條是從上半段的 TEXT TO SPEECH 橫跨過來拉進來的線；另一條是從下半段的 變數分配 拉進來的線。
回覆內容輸入框設定：點開這顆積木，直接在輸入框內塞入以下這兩個大括號變數：
Plaintext
{{#1_抽題解說老師.text#}}{{#2_答案批改與圖像.text#}}



成果驗收
當你把畫布整形成這條漂亮的結構後，請一口氣完成以下「驅魔三連擊」：
點擊畫布右上角的 「發布 (Publish)」 ➔ 「更新」。
回到右側的預覽對話視窗，點擊右上角那個帶有時鐘的 ↺（重置對話 / 清空歷史快取） 圖標，把之前的舊靜音快取和髒變數全部沖走。
出題測試：在輸入框輸入乾淨的 go 送出。
➔ 畫面上立刻彈出精美繁體中文多益考題表格（隱形變數生效），且因為脫離了分支，網頁前端會同步蹦出音軌播放條，耳機裡瞬間響起 tts-1 字正腔圓的純英文單字朗讀！
批改測試：依序輸入正確的單字答案送出。
➔ 畫面秒切換為批改報告，並在底端溫馨提示「打 go 繼續」。後台保險箱也透過「覆寫」穩穩記住了你答對的字。
排除測試：再次輸入 go。
➔ AI 老師拿起保險箱清單，精準剔除所有你剛才答對的單字，一秒為你奉上全新的下一組多益字彙與純英文語音！
參考資料:
AI Agent 實戰全攻略：Low-Code × 真落地，從地端部署到 RAG 設計，打造不被取代的核心競爭力（全書搭配 10 小時免費線上課程，讓你即戰力 UP！）
15 大商業主題，精選多益高頻單字
https://prepedu.com/zh-hant/blog/toeic-vocabulary-by-topics#toc-heading-9
Python 新手的 AI 之旅：從零開始打造屬於你的 AI / LLM 應用 - 
Dify 入門與工作流
https://ithelp.ithome.com.tw/users/20168885/ironman/7699
